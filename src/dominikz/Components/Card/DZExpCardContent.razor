@if (Parent?.Expanded ?? false)
{
    @ChildContent
}

@code {

    [Parameter]
    public RenderFragment ChildContent { get; set; }

    [CascadingParameter]
    protected internal DZExpCard Parent { get; set; }
}
